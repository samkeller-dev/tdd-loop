# 04 — LRU Cache

Difficulty: medium. Stateful class with eviction order. Common wrong
attempts: insertion order instead of recency order; `get` not refreshing
recency; `put` on an existing key not refreshing. Expected to solve in
1–3 attempts.
