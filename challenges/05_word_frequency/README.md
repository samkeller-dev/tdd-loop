# 05 — Word Frequency

Difficulty: easy-medium. Common wrong attempts: case-sensitive counting;
`str.split` only on space (not whitespace runs); blanket `isalpha` filter
that drops `"don't"`; emitting empty-string keys. Expected to solve in
1–2 attempts.
